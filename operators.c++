#include <iostream>

using namespace std;

int main(){
    int a = 2, b = 3;
    cout <<"a equals to: "<< a <<", b equals to: "<<b<<endl;
    cout<<"the sum of both numbres is: "<<a+b<<endl;
    cout<<"the multiplicity of both numbers is: "<<a*b<<endl;
    cout<<"the division of both numbers is: "<<a/b<<endl;
    cout<<sizeof(a);
}